name: 'Fork acrtion'
description: 'Create new issue on ropository fork'
author: 'Mozzart**'
inputs:
  repo-token:
    description: 'Token for the repository. Can be passed in using {{ secrets.GITHUB_TOKEN }}'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
